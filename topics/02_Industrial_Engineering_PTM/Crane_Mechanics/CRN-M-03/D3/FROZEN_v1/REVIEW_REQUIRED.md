# REVIEW REQUIRED — CRN-M-03 D3 FROZEN v1

This freeze is immutable.

Re-open D3 only through a new version if one of the following becomes material:

- a source/watchlist change invalidates a frozen claim boundary;
- an accepted D3 artifact has a proven technical or traceability defect;
- the ISO 17096 grab/grab-bucket scope boundary changes materially;
- a schema/verification change cannot be represented without altering frozen content.

Do not edit `FROZEN_v1` and do not move `crn-m-03-d3-v1`.

Any correction requires `FROZEN_v2` plus a new immutable tag.
