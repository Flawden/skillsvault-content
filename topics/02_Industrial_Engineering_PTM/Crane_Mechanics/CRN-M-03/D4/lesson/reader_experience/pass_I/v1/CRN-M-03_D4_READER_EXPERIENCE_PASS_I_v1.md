# CRN-M-03 / D4 — Reader Experience Pass I v1

## Decision

`PASS WITH READER-LAYER IMPROVEMENTS APPLIED`

Baseline remote commit:

`0a73d054a8203524db26e7df6f363f0e46831127`

Scope:

- 9 / 9 Parts;
- 45 / 45 Blocks;
- Technical Editorial Pass I complete;
- Red Team Pass I complete;
- technical depth and source/method/authority guards preserved;
- no new normative values, proof claims or equipment permissions introduced.

## Reader question

Can an engineer read M-03 D4 as **one executable expert workflow** instead of carrying 45 blocks, multiple source roles and the hook/grab scope traps in working memory?

This pass improves navigation, retrieval and positive causal flow. It does not simplify the evidence standard.

---

# Findings and improvements

## RX-M03-D4-01 — The 9-part route was not compactly visible as one workflow

**Severity:** HIGH

Part I now opens with a whole-D4 map:

```text
QUESTION / DECISION
→ CONFIGURATION / STATE / OUTPUT
→ SIMPLEST ADEQUATE MODEL
→ SOURCE + COMPONENT APPLICABILITY
→ HOOK / SUSPENSION / GRAB METHOD HIERARCHY
→ VERIFICATION / CROSS-CHECK / VALIDATION
→ SENSITIVITY / UNCERTAINTY / COVERAGE
→ INSPECTION / DUTY / MONITORING BY ROLE
→ BOUNDED REVIEW
→ STOP / AUTHORITY HANDOFF
```

Part IX repeats the route as a capstone.

---

## RX-M03-D4-02 — Every Part needed one observable capability target

**Severity:** HIGH

Parts I–IX now state `К концу Части ... ты должен уметь...`.

Targets are operational:

- classify question/method/source role;
- formulate a hook/grab boundary-value problem;
- choose model fidelity without deleting governing method requirements;
- map ISO 17440 direct/conditional/excluded scope safely;
- connect global/component/grab models without source laundering;
- separate verification/cross-check/validation;
- reason about sensitivity and coverage;
- integrate inspection/duty/monitoring by role and observability;
- write bounded review and authority handoff.

---

## RX-M03-D4-03 — Guard-dense Parts could feel like a prohibition list

**Severity:** HIGH

Parts II–VIII now start with a **positive causal line**: what the engineer is trying to build before the guard is applied.

Examples:

```text
REAL ASSEMBLY
→ SYSTEM BOUNDARY
→ STATE / CONTACTS / DOF
→ INPUT FREEZE
→ CALCULABLE MODEL
```

and

```text
QUESTION
→ SOURCE / COMPONENT MATCH
→ METHOD BRANCH
→ EXACT INPUTS
→ FORMAL RESULT
→ BOUNDED STATUS
```

The guards remain; their purpose becomes visible.

---

## RX-M03-D4-04 — The reader could waste effort memorizing hybrid English/Russian labels and standard numbers

**Severity:** MEDIUM

Part I now explicitly says:

- do not memorize every ISO number;
- do not memorize every English label;
- reconstruct the engineering distinction in your own words;
- return to exact source/version only when applicability or source-bound proof is at stake.

Part IV reinforces the memory rule:

```text
DIRECT
/ CONDITIONAL
/ EXCLUDED
```

before standard-number recall.

---

## RX-M03-D4-05 — Existing protocols lacked a compact entry-card route

**Severity:** HIGH

Every Part now has a short `Reader Navigator` telling the reader which record to create first:

- question/method card;
- system/boundary card;
- fidelity/adequacy card;
- source/component applicability card;
- parent/child/grab interface card;
- V&V evidence card;
- sensitivity/coverage card;
- evidence-role/observability card;
- bounded-review/handoff card.

No technical protocol is removed.

---

## RX-M03-D4-06 — Verification / cross-check / validation remained cognitively expensive

**Severity:** HIGH

Part VI now exposes a compact memory table:

| Layer | Question | What it does not prove |
|---|---|---|
| Verification | did we solve/implement the stated model correctly? | that the model represents the real system |
| Cross-check | does an independent reasoning/model route agree? | that common-mode error is excluded |
| Validation | is the model adequate for the real system and intended-use domain? | authority outside the validated domain |

The Red-Team common-mode guard remains explicit.

---

## RX-M03-D4-07 — Hook/grab source boundaries needed one compact visual memory rule

**Severity:** HIGH

Parts IV–V now show:

```text
ISO 17440:
DIRECT COVERAGE
→ CONDITIONAL EXTENSION ONLY WITH EXACT CONDITIONS
→ EXCLUDED / NOT COVERED

ISO 17096:
C-HOOK COVERAGE
≠ CRANE HOOK-SUSPENSION PROOF
≠ GRAB / GRAB-BUCKET AUTHORITY
```

This is a retrieval aid, not a new source claim.

---

## RX-M03-D4-08 — Inspection / monitoring / classification / proof / operation needed role-first routing

**Severity:** HIGH

Part VIII now presents evidence channels by the question they answer:

- inspection → observed condition within method/coverage;
- monitoring → recorded history within coverage/resolution;
- classification → duty context;
- proof method → formal source-bound check;
- safe-use/regulatory channel → operational/authority requirements.

Channels do not vote. Conflicts trigger state/scope/observability/method review.

---

## RX-M03-D4-09 — Retrieval was block-local but not Part-level

**Severity:** MEDIUM

Every Part now ends with a `Reader Checkpoint` requiring reconstruction, not vocabulary recall.

Part IX ends with a capstone reconstruction of the complete M-03 D4 route and hands off to Practice.

---

# Guard preservation

Reader Experience Pass I preserves the reviewed invariants:

```text
PRELIMINARY MATCH ≠ FINAL APPLICABILITY
OBSERVED / AS-FOUND ≠ APPROVED / DESIGN-CONFORMING
MODEL FORM SUFFICIENT ≠ GOVERNING METHOD SATISFIED
CONDITIONAL EXTENSION ≠ AUTOMATIC APPLICABILITY
ISO 17096 C-HOOK COVERAGE ≠ CRANE HOOK-SUSPENSION PROOF
ISO 17096 ≠ GRAB / GRAB-BUCKET AUTHORITY
SOLVER CONVERGED ≠ MODEL VALIDATED
TWO MODELS AGREE ≠ COMMON-MODE ERROR EXCLUDED
ROBUST IN TESTED/SOURCED ENVELOPE ≠ GLOBALLY ROBUST
NO DEFECT OBSERVED ≠ DEFECT ABSENT
NO EVENT LOGGED ≠ EVENT DID NOT OCCUR
ANALYSIS STOPPED ≠ OPERATION PERMITTED
TECHNICAL RECOMMENDATION ≠ AUTHORIZED EQUIPMENT DECISION
```

Still locked:

- closed paid tables, coefficients and proprietary method reconstruction;
- actual source-bound hook/component proof without exact applicable method and inputs;
- grab holding/capacity/acceptance verdict without exact applicable source/OEM/designer basis;
- numeric remaining-life verdict;
- discard/acceptance criteria imported from secondary sources;
- specialist NDT interpretation;
- equipment-specific repair/redesign/approval or operation authority.

## Whole-lesson reader route

```text
CLASSIFY THE QUESTION / DECISION
→ LOCK CONFIGURATION / STATE / OUTPUT
→ CHOOSE THE SIMPLEST ADEQUATE MODEL
→ PROVE SOURCE + COMPONENT APPLICABILITY
→ MAP HOOK / SUSPENSION / GRAB METHOD HIERARCHY
→ VERIFY IMPLEMENTATION
→ CROSS-CHECK + VALIDATE IN DOMAIN
→ CHALLENGE SENSITIVITY / UNCERTAINTY / COVERAGE
→ INTEGRATE INSPECTION / DUTY / MONITORING BY ROLE
→ WRITE A BOUNDED REVIEW
→ STOP OR HAND OFF TO THE ACTUAL AUTHORIZED PROCESS
```

## Verdict

`COMPLETE — PASS I`

D4 remains expert/reference depth. The pass makes the route easier to execute; it does not lower the standard of proof.

## Next

`CRN-M-03 / D4 PRACTICE / v1 — build expert integrated cases for question/method selection, hook/grab component scope, boundary/input provenance, V&V, sensitivity/error, evidence-role conflicts, source gaps and bounded authority handoff`
